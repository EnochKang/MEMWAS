% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_MEMWAS.R
\name{set_MEMWAS}
\alias{set_MEMWAS}
\title{Configure a MEMWAS mixed-effects analysis}
\usage{
set_MEMWAS(
  formula,
  family = "gaussian",
  data,
  id,
  time,
  random = ~1,
  autocor = "AR(1)",
  L1_penalty = 0,
  L2_penalty = 0,
  control = list(),
  method = "ML",
  random_cov = c("diagonal", "unstructured"),
  approximation = "laplace",
  init_approximation = "variational_inference",
  se_method = "hessian",
  dot_predictors = NULL,
  dot_alternative = NULL,
  dot_threshold = 0,
  dot_alpha = 0.05,
  nonlinear_alpha = 0.05,
  spline_knots = 4L,
  spline_probs = NULL,
  nonlinear_predictors = NULL,
  screen_nonlinear = TRUE,
  autocorrelation_check = "All",
  distribution_link_check = "All",
  conditional_independence_check = "All",
  random_effects_normality_check = "All",
  random_effects_predictor_independence_check = "All",
  homogeneity_variance_check = "All",
  engine = c("R", "cpp"),
  serial = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{formula}{formula. Fixed-effect model formula such as \code{y ~ x1 + x2}; simple numeric fixed-effect terms may be screened for spline non-linearity.}

\item{family}{character or family. Response family. Supported options are \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"negative_binomial"}, \code{"gamma"}, and \code{"exponential"}; aliases include \code{"normal"}, \code{"logit"}, \code{"logistic"}, \code{"count"}, \code{"negbin"}, \code{"nb"}, and \code{"exp"}.}

\item{data}{data.frame. Long-format analysis data containing the response, predictors, subject identifier, time variable, and any random-effect variables.}

\item{id}{character. Name of the subject identifier column; grouped operations use this column to keep repeated measurements together.}

\item{time}{character. Name of the within-subject measurement-time column used for sorting and autocorrelation structures.}

\item{random}{formula or character. Random-effect design specification; \code{~ 1} fits a random intercept and strings such as \code{"~ 1 + time"} are accepted. A mixed-model style term like \code{~ 1 + time | id} is reduced to its left-hand random-effect design.}

\item{autocor}{character. One legacy within-subject residual correlation structure. Supported options include \code{"NONE"}, \code{"AR(1)"}, \code{"AR(p)"}, \code{"ARMA(1,1)"}, \code{"CS"}, \code{"TOEP"}, and \code{"UN"}. A fully named character vector may also be used as multiple-component shorthand.}

\item{L1_penalty}{numeric. Non-negative L1 fixed-effect penalty. The intercept is not penalized.}

\item{L2_penalty}{numeric. Non-negative L2 fixed-effect penalty. The intercept is not penalized.}

\item{control}{list. Advanced controls passed to fitting and screening, including optimizer controls, approximation controls, \code{continuous_time}, \code{toep_lags}, \code{max_unstructured_dim}, \code{spline_grid}, and \code{min_unique_nonlinear}. Family-specific entries include \code{negative_binomial_theta}, \code{gamma_shape}, \code{estimate_dispersion}, \code{aghq_nodes}, \code{aghq_max_dim}, \code{bootstrap_B}, and profile-likelihood controls.}

\item{method}{character. Likelihood method for Gaussian models: \code{"ML"} or \code{"REML"}. Non-Gaussian models are fitted with approximate ML-style working likelihoods and are forced to \code{"ML"}.}

\item{random_cov}{character. Random-effect covariance parameterization: \code{"diagonal"} or \code{"unstructured"}.}

\item{approximation}{character. Final non-Gaussian approximation strategy. The default is \code{"laplace"} after variational-inference initialization. Supported options are \code{"laplace"}, \code{"variational_inference"}, \code{"adaptive_gauss_hermite_quadrature"}, \code{"adaptive_gaussian_quadrature"}, \code{"saddlepoint"}, \code{"skew_corrected_laplace"}, and \code{"pql"}.}

\item{init_approximation}{character. Initial approximation for non-Gaussian models. Defaults to \code{"variational_inference"}; this is used for starting values before final Laplace/AGHQ/saddlepoint/skew-corrected fitting.}

\item{se_method}{character. Standard-error method for non-Gaussian final fits: \code{"hessian"}, \code{"cluster_sandwich"}, \code{"parametric_bootstrap"}, \code{"profile"}, or a character vector.}

\item{dot_predictors}{character. Optional fixed-effect coefficient terms for directional one-tailed tests (DOT). Names should match rows in the fixed-effect coefficient table, such as \code{"x1"} or \code{"groupB"}.}

\item{dot_alternative}{character, named character, list, or data.frame. DOT alternative(s): \code{"greater"} tests \code{beta > abs(dot_threshold)}, \code{"less"} tests \code{beta < -abs(dot_threshold)}, and \code{"two.sided"} uses the usual two-sided Wald test when \code{dot_threshold = 0} or a two-sided minimum-effect test of \verb{|beta| > abs(dot_threshold)} when \code{dot_threshold > 0}. Named values override specific terms; a scalar value applies to \code{dot_predictors}, or to all non-intercept fixed-effect coefficient terms when \code{dot_predictors} is \code{NULL}. Data frames may be supplied to \code{dot_alternative}, \code{dot_threshold}, or \code{dot_alpha} with a \code{term}/\code{predictor} column plus relevant \code{alternative}, \code{threshold}/\code{dot_threshold}, or \code{alpha} columns.}

\item{dot_threshold}{numeric or data.frame. Predictor-specific minimally important value(s). Values are interpreted as magnitudes and converted with \code{abs()}: \code{"greater"} tests against \code{+abs(dot_threshold)}, \code{"less"} tests against \code{-abs(dot_threshold)}, and \code{"two.sided"} with a nonzero value tests whether the absolute effect exceeds that magnitude. Use a scalar, a vector aligned with \code{dot_predictors}, a named vector/list keyed by coefficient or model term, or a data frame with \code{term}/\code{predictor} and \code{threshold}/\code{dot_threshold} columns. Defaults to 0.}

\item{dot_alpha}{numeric. Significance level(s) for DOT. Use a scalar, a vector aligned with \code{dot_predictors}, or a named vector/list keyed by coefficient term. Defaults to 0.05.}

\item{nonlinear_alpha}{numeric. Significance threshold for likelihood-ratio screening of spline terms.}

\item{spline_knots}{integer. Number of restricted cubic spline knots; must be at least 3.}

\item{spline_probs}{numeric. Optional quantile probabilities used to place spline knots; length must equal \code{spline_knots}.}

\item{nonlinear_predictors}{character. Optional subset of fixed-effect predictor names to screen for non-linearity.}

\item{screen_nonlinear}{logical. Whether to perform spline screening during setup.}

\item{autocorrelation_check}{character or logical. Base-R residual autocorrelation screening method(s). Use \code{"All"}, \code{"None"}, \code{TRUE}, \code{FALSE}, or any combination of \code{"DurbinWatson"}, \code{"LjungBox"}, \code{"Lag1Correlation"}, and \code{"Runs"}. Default \code{"All"} runs all listed tests.}

\item{distribution_link_check}{character or logical. Base-R screening method(s) for the GLMM response distribution and link function. Use \code{"All"}, \code{"None"}, or any combination of \code{"PearsonDispersion"}, \code{"DevianceDispersion"}, \code{"LinkTest"}, \code{"QuantileResidualNormality"}, and \code{"GroupedCalibration"}. Default \code{"All"}.}

\item{conditional_independence_check}{character or logical. Base-R conditional residual-independence screening method(s). Use \code{"All"}, \code{"None"}, or any combination of \code{"WithinClusterLag1"}, \code{"ClusterMeanResidual"}, and \code{"Runs"}. Default \code{"All"}.}

\item{random_effects_normality_check}{character or logical. Base-R screening method(s) for empirical random-effect normality. Use \code{"All"}, \code{"None"}, or any combination of \code{"Shapiro"}, \code{"JarqueBera"}, and \code{"SkewKurtosis"}. Default \code{"All"}.}

\item{random_effects_predictor_independence_check}{character or logical. Base-R screening method(s) for independence between empirical random effects and predictors. Use \code{"All"}, \code{"None"}, or any combination of \code{"GroupMeanAssociation"}, \code{"Correlation"}, and \code{"RankCorrelation"}. Default \code{"All"}.}

\item{homogeneity_variance_check}{character or logical. Base-R screening method(s) for homogeneous variance / scale. Use \code{"All"}, \code{"None"}, or any combination of \code{"BreuschPagan"}, \code{"White"}, \code{"LeveneFitted"}, and \code{"LeveneGroup"}. Default \code{"All"}.}

\item{engine}{character. Execution engine: \code{"R"} runs all calculations in base R; \code{"cpp"} routes available numerical helpers, linear algebra, spline bases, penalties, metrics, and optimization kernels through registered C++ \code{.Call} routines with no Rcpp dependency.}

\item{serial}{Optional serial-process component or named list/vector of components created with \code{serial_component()}. Each component has its own one-column design and autocorrelation structure. Do not combine this with a non-\code{"NONE"} scalar \code{autocor} value.}

\item{verbose}{logical. Whether to print screening progress messages.}

\item{...}{list. Additional user metadata stored in the returned settings object.}
}
\value{
MEMWAS. A settings object containing validated inputs, spline-screening results, selected spline metadata, GLMM assumption-screening specifications and results, and fitting controls.
}
\description{
Validate model inputs, optionally screen numeric fixed-effect predictors for non-linearity with restricted cubic splines, and return a \code{MEMWAS} settings object for \code{fit_MEMWAS()} or \code{tune_MEMWAS()}.
}
\details{
\code{set_MEMWAS()} is the configuration layer for MEMWAS. It normalizes the response family, likelihood method, random-effect covariance option, and residual autocorrelation structure; checks that formula variables exist; optionally performs one-predictor-at-a-time restricted-cubic-spline screening; stores selected spline bases for the final formal fit, and performs dependency-free screening of six GLMM assumptions from the baseline mixed-effects screening model.

Algorithm choices are controlled primarily by \code{family}, \code{method}, \code{random_cov}, \code{autocor}/\code{serial}, \code{approximation}, and \code{engine}. Gaussian models use marginal ML or REML. Binomial, Poisson, negative-binomial, gamma, and exponential models use variational-inference initialization followed by the selected native-C++ marginal approximation inside \code{fit_MEMWAS()}, so non-linearity screening for those families is approximate and is labeled by the selected final approximation method. Autocorrelation choices include independence, AR-type, compound-symmetry, Toeplitz, and unstructured residual correlation structures. When \code{engine = "cpp"}, numerical helper routines are dispatched to native C++ implementations registered with \code{.Call}; model orchestration, formula handling, and S3 object construction remain in R.
}
\examples{
\dontrun{
dat <- simulate_panel_data(n_id = 100, n_time = 5)
setup <- set_MEMWAS(var_1 ~ var_2 + var_3, data = dat, id = "id", time = "time",
                    family = "gaussian", autocor = "AR(1)", engine = "cpp")
fit <- fit_MEMWAS(setup)
}
}
