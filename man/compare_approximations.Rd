% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approximation-tools.R
\name{compare_approximations}
\alias{compare_approximations}
\title{Compare MEMWAS approximation methods}
\usage{
compare_approximations(
  object = NULL,
  approximations = c("laplace", "adaptive_gauss_hermite_quadrature", "saddlepoint",
    "skew_corrected_laplace"),
  init_approximation = "variational_inference",
  se_method = "hessian",
  refit = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{MEMWAS} settings object, a \code{MEMWAS_fit} object, a formula,
or \code{NULL} when formula/data/id/time/random are supplied through \code{...}.}

\item{approximations}{Character vector of final approximation methods to compare.}

\item{init_approximation}{Initial approximation used for all refits. Defaults
to \code{"variational_inference"}.}

\item{se_method}{Standard-error method used in the refits. Defaults to \code{"hessian"}.}

\item{refit}{logical. Whether to refit models. Currently must be \code{TRUE}.}

\item{...}{Arguments passed to \code{fit_MEMWAS()} when \code{object} is a formula or
\code{NULL}, or override arguments passed to \code{fit_MEMWAS()} for each MEMWAS
settings refit.}
}
\value{
A list of class \code{MEMWAS_approximation_comparison} with refitted
models, fit-level metrics, coefficient tables in long form, and per-term
sensitivity ranges.
}
\description{
Refit the same MEMWAS model with several non-Gaussian
approximation strategies and return coefficient-level and fit-level
sensitivity summaries. This is useful for checking whether scientific
conclusions, directional one-tailed test decisions, or standard errors are
sensitive to the final approximation.
}
\examples{
\dontrun{
cmp <- compare_approximations(
  setup,
  approximations = c("laplace", "adaptive_gauss_hermite_quadrature")
)
print(cmp)
}
}
