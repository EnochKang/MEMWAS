% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_MEMWAS.R
\name{fit_MEMWAS}
\alias{fit_MEMWAS}
\title{Fit a MEMWAS mixed-effects model}
\usage{
fit_MEMWAS(
  object = NULL,
  formula = NULL,
  family = NULL,
  data = NULL,
  id = NULL,
  time = NULL,
  random = NULL,
  autocor = NULL,
  serial = NULL,
  L1_penalty = NULL,
  L2_penalty = NULL,
  control = NULL,
  method = NULL,
  random_cov = NULL,
  approximation = NULL,
  init_approximation = NULL,
  se_method = NULL,
  dot_predictors = NULL,
  dot_alternative = NULL,
  dot_threshold = 0,
  dot_alpha = 0.05,
  spline_variables = NULL,
  spline_info = NULL,
  turning_points = NULL,
  nonlinear_summary = NULL,
  all_screened_spline_info = NULL,
  baseline_screen_metrics = NULL,
  engine = NULL,
  verbose = NULL,
  ...
)
}
\arguments{
\item{object}{MEMWAS or formula or NULL. A settings object returned by \code{set_MEMWAS()}, a model formula, or \code{NULL} when all required settings are supplied manually.}

\item{formula}{formula. Fixed-effect model formula; ignored when \code{object} is a populated \code{MEMWAS} settings object unless explicitly supplied as an override.}

\item{family}{character or family. Response family. Supported options are \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"negative_binomial"}, \code{"gamma"}, and \code{"exponential"} plus aliases.}

\item{data}{data.frame. Long-format analysis data.}

\item{id}{character. Name of the subject identifier column.}

\item{time}{character. Name of the measurement-time column.}

\item{random}{formula or character. Random-effect design, such as \code{~ 1} or \code{~ 1 + time}.}

\item{autocor}{character. One legacy residual autocorrelation structure: \code{"NONE"}, \code{"AR(1)"}, \code{"AR(p)"}, \code{"ARMA(1,1)"}, \code{"CS"}, \code{"TOEP"}, or \code{"UN"}. A fully named character vector may also define multiple components.}

\item{serial}{Optional serial-process component or named list/vector of components created with \code{serial_component()}. Each component contributes \verb{diag(h_k) u_k} and has its own covariance structure.}

\item{L1_penalty}{numeric. Non-negative L1 fixed-effect penalty; the intercept is unpenalized.}

\item{L2_penalty}{numeric. Non-negative L2 fixed-effect penalty; the intercept is unpenalized.}

\item{control}{list. Fitting controls, including optimizer controls, approximation controls, coordinate-descent controls, spline grid size, and autocorrelation regularization settings. Family-specific entries include \code{negative_binomial_theta}, \code{gamma_shape}, \code{aghq_nodes}, \code{aghq_max_dim}, \code{bootstrap_B}, and profile-likelihood controls.}

\item{method}{character. \code{"ML"} or \code{"REML"} for Gaussian models; non-Gaussian models use \code{"ML"} with the selected approximation.}

\item{random_cov}{character. Random-effect covariance structure: \code{"diagonal"} or \code{"unstructured"}.}

\item{approximation}{character. Final non-Gaussian approximation strategy. The default is \code{"laplace"} after variational-inference initialization. Supported options are \code{"laplace"}, \code{"variational_inference"}, \code{"adaptive_gauss_hermite_quadrature"}, \code{"adaptive_gaussian_quadrature"}, \code{"saddlepoint"}, \code{"skew_corrected_laplace"}, and \code{"pql"}.}

\item{init_approximation}{character. Initial approximation used to generate starting values for non-Gaussian final optimization. Defaults to \code{"variational_inference"}.}

\item{se_method}{character. Standard-error method for non-Gaussian final fits. Use \code{"hessian"}, \code{"cluster_sandwich"}, \code{"parametric_bootstrap"}, \code{"profile"}, or a character vector combining these options.}

\item{dot_predictors}{character. Optional fixed-effect coefficient terms for directional one-tailed tests (DOT). Names should match rows in the fixed-effect coefficient table, such as \code{"x1"} or \code{"groupB"}.}

\item{dot_alternative}{character, named character, list, or data.frame. DOT alternative(s): \code{"greater"} tests \code{beta > abs(dot_threshold)}, \code{"less"} tests \code{beta < -abs(dot_threshold)}, and \code{"two.sided"} uses the usual two-sided Wald test when \code{dot_threshold = 0} or a two-sided minimum-effect test of \verb{|beta| > abs(dot_threshold)} when \code{dot_threshold > 0}. Named values override specific terms; a scalar value applies to \code{dot_predictors}, or to all non-intercept fixed-effect coefficient terms when \code{dot_predictors} is \code{NULL}. Data frames may be supplied to \code{dot_alternative}, \code{dot_threshold}, or \code{dot_alpha} with a \code{term}/\code{predictor} column plus relevant \code{alternative}, \code{threshold}/\code{dot_threshold}, or \code{alpha} columns.}

\item{dot_threshold}{numeric or data.frame. Predictor-specific minimally important value(s). Values are interpreted as magnitudes and converted with \code{abs()}: \code{"greater"} tests against \code{+abs(dot_threshold)}, \code{"less"} tests against \code{-abs(dot_threshold)}, and \code{"two.sided"} with a nonzero value tests whether the absolute effect exceeds that magnitude. Use a scalar, a vector aligned with \code{dot_predictors}, a named vector/list keyed by coefficient or model term, or a data frame with \code{term}/\code{predictor} and \code{threshold}/\code{dot_threshold} columns. Defaults to 0.}

\item{dot_alpha}{numeric. Significance level(s) for DOT. Use a scalar, a vector aligned with \code{dot_predictors}, or a named vector/list keyed by coefficient term. Defaults to 0.05.}

\item{spline_variables}{character. Names of predictors whose stored spline bases should be included.}

\item{spline_info}{list. Stored spline knot and basis-name metadata, usually produced by \code{set_MEMWAS()}.}

\item{turning_points}{list. Optional turning-point metadata from spline screening.}

\item{nonlinear_summary}{data.frame. Optional spline-screening summary table.}

\item{all_screened_spline_info}{list. Optional metadata for all screened spline predictors.}

\item{baseline_screen_metrics}{list. Optional fit metrics from the baseline screening model.}

\item{engine}{character. Execution engine: \code{"R"} uses base-R numerical helpers; \code{"cpp"} uses native C++ helpers for available numerical operations including safe exponentials, diagonals, linear solves, Cholesky factorizations, penalty kernels, coordinate descent, lag matrices, AR helper calculations, spline bases, and other calculation kernels.}

\item{verbose}{logical. Whether to print progress messages.}

\item{...}{list. Additional overrides or metadata.}
}
\value{
MEMWAS_fit. A fitted model object with coefficients, standard errors, fitted values, residuals, random effects, covariance estimates, autocorrelation estimates, fit metrics, convergence information, base-R GLMM assumption-screening tables, and original settings.
}
\description{
Fit the MEMWAS mixed-effects model specified by a \code{MEMWAS} settings object or by direct formula/data arguments, with optional fixed-effect elastic-net penalties, random effects, residual autocorrelation, and stored spline terms.
}
\details{
\code{fit_MEMWAS()} first merges a settings object with any explicit user overrides and validates formula, data, subject, time, penalty, method, random covariance, and engine options. It then constructs fixed- and random-effect design matrices from R formulas and fits a marginal mixed-effects model.

For \code{family = "gaussian"}, the function optimizes the marginal Gaussian likelihood, or REML when \code{method = "REML"} and no fixed-effect penalties are used. For \code{family = "binomial"}, \code{"poisson"}, \code{"negative_binomial"}, \code{"gamma"}, and \code{"exponential"}, the function uses variational inference as the default initializer and then evaluates the requested final non-Gaussian marginal approximation with native C++ subject-level kernels. Negative-binomial and gamma dispersion parameters are treated as likelihood parameters unless disabled in \code{control}. Fixed effects can be estimated by ridge or elastic-net style penalties via \code{L1_penalty} and \code{L2_penalty}; if an L1 penalty is present, coordinate descent is used for the fixed-effect subproblem.

Residual correlation is selected with \code{autocor}. \code{"AR(1)"}, higher-order AR, ARMA(1,1), compound-symmetry, Toeplitz, and unstructured structures are available. Random-effect covariance is controlled by \code{random_cov}. The \code{engine} argument changes only the calculation backend: formula parsing, S3 output, and model assembly stay in R, while numerical helper routines are routed to C++ when \code{engine = "cpp"}.
}
\examples{
\dontrun{
dat <- simulate_panel_data(n_id = 100, n_time = 5)
fit <- fit_MEMWAS(var_1 ~ var_2 + var_3, family = "gaussian", data = dat,
                  id = "id", time = "time", random = ~ 1,
                  autocor = "AR(1)", engine = "R")
summary(fit)
}
}
