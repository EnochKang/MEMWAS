% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_MEMWAS.R
\name{tune_MEMWAS}
\alias{tune_MEMWAS}
\title{Tune MEMWAS elastic-net penalties by grouped K-fold cross-validation}
\usage{
tune_MEMWAS(
  object = NULL,
  formula = NULL,
  family = NULL,
  data = NULL,
  id = NULL,
  time = NULL,
  random = NULL,
  autocor = NULL,
  control = NULL,
  method = NULL,
  random_cov = NULL,
  approximation = NULL,
  init_approximation = NULL,
  se_method = NULL,
  dot_predictors = NULL,
  dot_alternative = NULL,
  dot_threshold = 0,
  dot_alpha = 0.05,
  engine = NULL,
  K = 5L,
  metric = c("MAE", "MSE", "RMSE", "MAPE", "SMAPE"),
  stability_metric = c("IQR", "SD", "variance"),
  surrogate = c("QRS", "GP"),
  use_stability_penalty = TRUE,
  lambda_log_range = c(-6, 2),
  lambda_base = 10,
  alpha_range = c(0, 1),
  initial_design = c("lhs", "boundary"),
  n_initial = 10L,
  initial_points = NULL,
  max_iter = 20L,
  improvement_tol = 1e-06,
  patience = 3L,
  candidate_grid_size = 1000L,
  exploration = 1,
  stability_weight = 1,
  seed = NULL,
  refit_final = TRUE,
  autocor_regularization = NULL,
  autocor_regularization_type = c("L2", "L1", "elasticnet", "none"),
  autocor_regularization_alpha = 0,
  mape_epsilon = .Machine$double.eps,
  fail_value = Inf,
  keep_fold_fits = FALSE,
  serial = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{MEMWAS or formula or NULL. A settings object from \code{set_MEMWAS()}, a formula, or \code{NULL} when direct model arguments are supplied.}

\item{formula}{formula. Fixed-effect model formula used when \code{object} is not a populated settings object.}

\item{family}{character or family. Response family: \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"negative_binomial"}, \code{"gamma"}, or \code{"exponential"}.}

\item{data}{data.frame. Long-format analysis data.}

\item{id}{character. Subject identifier column used for grouped cross-validation folds.}

\item{time}{character. Measurement-time column.}

\item{random}{formula or character. Random-effect design.}

\item{autocor}{character. Legacy residual autocorrelation structure to use in each cross-validation fit, or a fully named multiple-component shorthand.}

\item{control}{list. Fitting, approximation, optimizer, spline, and autocorrelation controls passed to \code{fit_MEMWAS()}.}

\item{method}{character. \code{"ML"} or \code{"REML"} for Gaussian models; non-Gaussian models are evaluated with ML and the selected approximation.}

\item{random_cov}{character. Random-effect covariance option: \code{"diagonal"} or \code{"unstructured"}.}

\item{approximation}{character. Final non-Gaussian approximation strategy passed to each fold fit and final refit. The default is \code{"laplace"} after variational-inference initialization. Supported options are \code{"laplace"}, \code{"adaptive_gauss_hermite_quadrature"}, \code{"adaptive_gaussian_quadrature"}, \code{"saddlepoint"}, \code{"skew_corrected_laplace"}, \code{"variational_inference"}, and legacy \code{"pql"}.}

\item{init_approximation}{character. Initial approximation used for non-Gaussian fold fits and final refit. Defaults to \code{"variational_inference"}.}

\item{se_method}{character. Standard-error method for non-Gaussian final refits. Use \code{"hessian"}, \code{"cluster_sandwich"}, \code{"parametric_bootstrap"}, \code{"profile"}, or a character vector.}

\item{dot_predictors}{character. Optional fixed-effect coefficient terms for directional one-tailed tests (DOT) in the final refit and any stored fold fits. Names should match rows in the fixed-effect coefficient table.}

\item{dot_alternative}{character, named character, list, or data.frame. DOT alternative(s): \code{"greater"} tests \code{beta > abs(dot_threshold)}, \code{"less"} tests \code{beta < -abs(dot_threshold)}, and \code{"two.sided"} uses the usual two-sided Wald test when \code{dot_threshold = 0} or a two-sided minimum-effect test of \verb{|beta| > abs(dot_threshold)} when \code{dot_threshold > 0}. Named values override specific terms; a scalar value applies to \code{dot_predictors}, or to all non-intercept fixed-effect coefficient terms when \code{dot_predictors} is \code{NULL}. Data frames may be supplied to \code{dot_alternative}, \code{dot_threshold}, or \code{dot_alpha} with a \code{term}/\code{predictor} column plus relevant \code{alternative}, \code{threshold}/\code{dot_threshold}, or \code{alpha} columns.}

\item{dot_threshold}{numeric or data.frame. Predictor-specific minimally important value(s). Values are interpreted as magnitudes and converted with \code{abs()}: \code{"greater"} tests against \code{+abs(dot_threshold)}, \code{"less"} tests against \code{-abs(dot_threshold)}, and \code{"two.sided"} with a nonzero value tests whether the absolute effect exceeds that magnitude. Use a scalar, a vector aligned with \code{dot_predictors}, a named vector/list keyed by coefficient or model term, or a data frame with \code{term}/\code{predictor} and \code{threshold}/\code{dot_threshold} columns. Defaults to 0.}

\item{dot_alpha}{numeric. Significance level(s) for DOT. Use a scalar, a vector aligned with \code{dot_predictors}, or a named vector/list keyed by coefficient term. Defaults to 0.05.}

\item{engine}{character. Execution engine: \code{"R"} for base-R numerical helpers or \code{"cpp"} for registered native C++ helper routines in every cross-validation fit and tuning kernel where available.}

\item{K}{integer. Number of grouped cross-validation folds; all rows for a subject stay in one fold.}

\item{metric}{character. Prediction error metric to minimize: \code{"MAE"}, \code{"MSE"}, \code{"RMSE"}, \code{"MAPE"}, or \code{"SMAPE"}.}

\item{stability_metric}{character. Dispersion metric for fold stability: \code{"IQR"}, \code{"SD"}, or \code{"variance"}.}

\item{surrogate}{character. Surrogate model for proposing new penalty values: \code{"QRS"} for quadratic response surface or \code{"GP"} for a small Gaussian-process surrogate.}

\item{use_stability_penalty}{logical. Whether to add a stability penalty to the median cross-validation error.}

\item{lambda_log_range}{numeric. Two-element range for \code{log(lambda, base = lambda_base)}.}

\item{lambda_base}{numeric. Base used to transform log-lambda values to lambda.}

\item{alpha_range}{numeric. Two-element range for elastic-net mixing parameter \code{alpha}, constrained to \verb{[0, 1]}.}

\item{initial_design}{character. Initial design strategy: \code{"lhs"} for Latin-hypercube-like sampling plus boundaries or \code{"boundary"} for boundary points only.}

\item{n_initial}{integer. Number of initial penalty pairs to evaluate for the \code{"lhs"} design.}

\item{initial_points}{data.frame. Optional user-supplied starting points with \code{log_lambda} or \code{lambda}, and \code{alpha}.}

\item{max_iter}{integer. Maximum number of surrogate-guided search iterations after initial evaluations.}

\item{improvement_tol}{numeric. Minimum improvement in selection score considered meaningful.}

\item{patience}{integer. Stop after this many consecutive negligible-improvement iterations.}

\item{candidate_grid_size}{integer. Number of candidate penalty pairs sampled at each surrogate iteration.}

\item{exploration}{numeric. Exploration multiplier for surrogate uncertainty.}

\item{stability_weight}{numeric. Weight for the stability penalty in the selection score.}

\item{seed}{integer. Optional random seed for reproducible fold assignment and design generation.}

\item{refit_final}{logical. Whether to refit the best MEMWAS model on the full data set.}

\item{autocor_regularization}{numeric or list. Optional regularization applied to autocorrelation parameters during fitting.}

\item{autocor_regularization_type}{character. Autocorrelation regularization type: \code{"L2"}, \code{"L1"}, \code{"elasticnet"}, or \code{"none"}.}

\item{autocor_regularization_alpha}{numeric. Mixing value for elastic-net autocorrelation regularization.}

\item{mape_epsilon}{numeric. Positive stabilizer used in MAPE and SMAPE denominators.}

\item{fail_value}{numeric. Metric value assigned to failed folds.}

\item{keep_fold_fits}{logical. Whether to store all fold-level fitted model objects.}

\item{serial}{Optional serial-process component or named list/vector of components created with \code{serial_component()}. The same definitions are retained in every grouped fold and the final refit.}

\item{verbose}{logical. Whether to print tuning progress.}

\item{...}{list. Additional arguments passed to \code{fit_MEMWAS()}.}
}
\value{
tune_MEMWAS. A tuning object containing the best penalty pair, all observed results, fold-level cross-validation details, optional final fit, a structured \code{best_model} component with final formulas and coefficients, fold assignments, settings, tuning controls, and convergence diagnostics.
}
\description{
Tune MEMWAS fixed-effect L1/L2 penalties using subject-grouped K-fold cross-validation and a lightweight surrogate search over \code{lambda} and \code{alpha}. The returned tuning object now stores a structured \code{best_model} summary with final fixed-effect coefficients, random-effect BLUPs and covariance estimates, penalty formulas and values, and autocorrelation formulas and penalty values when the final model is refitted.
}
\details{
\code{tune_MEMWAS()} tunes fixed-effect penalties through the reparameterization \code{L1_penalty = alpha * lambda} and \code{L2_penalty = (1 - alpha) * lambda}. Cross-validation is grouped by subject ID, so no subject contributes rows to both training and validation within the same fold. The best row minimizes median validation error plus an optional stability penalty computed from fold-level error dispersion.

Search begins with boundary and optional Latin-hypercube-like initial points. It then fits either a quadratic response-surface surrogate (\code{surrogate = "QRS"}) or a compact Gaussian-process surrogate (\code{surrogate = "GP"}) over observed cross-validation results and proposes new penalty pairs with uncertainty-guided exploration. \code{metric}, \code{stability_metric}, \code{surrogate}, \code{lambda_log_range}, \code{alpha_range}, and \code{engine} are the main tuning algorithm controls.

With \code{engine = "cpp"}, available numerical work inside each MEMWAS fit and several tuning kernels, including metrics, fold assignment, and covariance-kernel calculations, are dispatched to compiled C++ routines through \code{.Call} without using Rcpp or other external packages.
}
\examples{
\dontrun{
dat <- simulate_panel_data(n_id = 50, n_time = 5)
setup <- set_MEMWAS(var_1 ~ var_2 + var_3, data = dat, id = "id", time = "time",
                    screen_nonlinear = FALSE)
tuned <- tune_MEMWAS(setup, K = 3, max_iter = 2, n_initial = 5, engine = "cpp")
tuned$best
}
}
