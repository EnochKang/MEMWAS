% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-multiple-serial-processes.R
\name{serial_component}
\alias{serial_component}
\title{Define one MEMWAS serial-process component}
\usage{
serial_component(
  structure = "AR(1)",
  predictor = NULL,
  design = NULL,
  order = NULL,
  name = NULL,
  control = list()
)
}
\arguments{
\item{structure}{Character scalar naming the covariance structure. Supported
values are \code{"NONE"}, \code{"AR(1)"}, \code{"EXP"}/\code{"OU"}, \code{"AR(p)"},
\code{"ARMA(1,1)"}, \code{"CS"}, \code{"TOEP"}, and \code{"UN"}.}

\item{predictor}{Optional character scalar naming one numeric predictor. It
is shorthand for \code{design = ~ 0 + predictor}.}

\item{design}{Optional one-sided formula that must create exactly one design
column. Use \code{~ 1} for an outcome-level residual process and \code{~ 0 + x} for
a predictor-specific process.}

\item{order}{Optional positive integer for AR(p) or TOEP structures.}

\item{name}{Optional descriptive component name. A name supplied by the
enclosing \code{serial = list(...)} takes precedence.}

\item{control}{Optional component-specific controls. These override matching
entries in the model-level \code{control} list for this component.}
}
\value{
An object of class \code{MEMWAS_serial_component} for use in
\code{fit_MEMWAS(serial = ...)} or \code{set_MEMWAS(serial = ...)}.
}
\description{
Define one MEMWAS serial-process component
}
