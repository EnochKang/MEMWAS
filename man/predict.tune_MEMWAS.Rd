% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-tune_MEMWAS.R
\name{predict.tune_MEMWAS}
\alias{predict.tune_MEMWAS}
\title{Predict from the best tuned MEMWAS model}
\usage{
\method{predict}{tune_MEMWAS}(
  object,
  newdata = NULL,
  type = c("link", "response"),
  include_random = FALSE,
  ...
)
}
\arguments{
\item{object}{tune_MEMWAS. Tuning object returned by \code{tune_MEMWAS()}.}

\item{newdata}{data.frame. Optional new data; when omitted, predictions are computed for the final model's original data.}

\item{type}{character. Prediction scale: \code{"link"} or \code{"response"}.}

\item{include_random}{logical. Whether to include random effects. This argument is passed to \code{predict.MEMWAS_fit()}; fixed-effect predictions are used for new data.}

\item{...}{list. Additional arguments passed to \code{predict.MEMWAS_fit()}.}
}
\value{
numeric. Predicted values from the best final MEMWAS model on the requested scale.
}
\description{
Generate link-scale or response-scale predictions from the final model refit stored inside a \code{tune_MEMWAS} object.
}
\examples{
\dontrun{
pred <- predict(tuned, type = "response")
}
}
